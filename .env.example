# ===========================================
# Configuration SMTP pour le formulaire de contact
# ===========================================
# Copiez ce fichier en .env.local et remplissez les valeurs

# Configuration du serveur SMTP
# Pour Gmail: smtp.gmail.com
# Pour OVH: ssl0.ovh.net
SMTP_HOST=smtp.gmail.com

# Port SMTP
# 587 pour TLS (recommandé)
# 465 pour SSL
# 25 pour non-sécurisé (déconseillé)
SMTP_PORT=587

# Utiliser SSL/TLS
# false pour port 587 (STARTTLS)
# true pour port 465 (SSL)
SMTP_SECURE=false

# Identifiants SMTP
# Pour Gmail: utilisez un "App Password" (pas votre mot de passe normal)
# https://support.google.com/accounts/answer/185833
SMTP_USER=votre-email@gmail.com
SMTP_PASS=votre-app-password

# Adresse expéditeur (optionnel, utilise SMTP_USER par défaut)
SMTP_FROM=contact@infojv.net

# Adresse destinataire des messages
SMTP_TO=contact@infojv.net
